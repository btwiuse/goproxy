# Upstream Go proxy target (default: https://proxy.golang.org)
# GOPROXY_TARGET=https://proxy.golang.org

# Listen address (default: :8090)
# Railway assigns the PORT variable automatically; the app reads GOPROXY_ADDR
# If you keep the default, expose port 8090 on Railway.
# GOPROXY_ADDR=:8090
